import { RolePlaceholder } from "@/components/shared/RolePlaceholder";

export default function AdminAssessmentOverviewPage() {
  return <RolePlaceholder eyebrow="Admin" title="Assessment overview shell" description="This overview will host version history, lifecycle state, ownership, and readiness indicators." />;
}
