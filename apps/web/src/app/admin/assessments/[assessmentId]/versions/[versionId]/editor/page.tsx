import { RolePlaceholder } from "@/components/shared/RolePlaceholder";

export default function AdminEditorPage() {
  return <RolePlaceholder eyebrow="Admin" title="Editor canvas shell" description="This page will become the main editor workspace with a central canvas and supporting inspector rails." />;
}
