import { RolePlaceholder } from "@/components/shared/RolePlaceholder";

export default function AdminLibraryPage() {
  return <RolePlaceholder eyebrow="Admin" title="Reusable library shell" description="This area will host reusable assessment components, templates, and reference content." />;
}
