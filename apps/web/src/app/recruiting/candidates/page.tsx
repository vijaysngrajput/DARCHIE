import { RolePlaceholder } from "@/components/shared/RolePlaceholder";

export default function RecruitingCandidatesPage() {
  return <RolePlaceholder eyebrow="Recruiting" title="Candidate pipeline shell" description="This area will hold candidate search, filters, scorecard summaries, and pipeline review inside the new shared shell." />;
}
