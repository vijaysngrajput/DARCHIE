import { RolePlaceholder } from "@/components/shared/RolePlaceholder";

export default function ReviewerQueuePage() {
  return <RolePlaceholder eyebrow="Reviewer" title="Review queue shell" description="This queue will become the intake view for assigned submissions, prioritization, and review status." />;
}
